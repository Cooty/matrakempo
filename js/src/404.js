require(['headerscroll',
         'drawermenu',
         'search'], function(headerScroll, drawerMenu, search) {
            headerScroll.init();
            drawerMenu.init();
            search.init();
    });
